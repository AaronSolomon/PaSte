#ifndef _UTILS_H_
#define _UTILS_H_
enum{
    ENUM_IP,
    ENUM_UDP,
    ENUM_TCP,
    ENUM_RTP
};

enum {
    ENUM_HELP = 0,
    ENUM_DEF,
    ENUM_GRAMMER,
    ENUM_SELECT,
    ENUM_INJECT,
    ENUM_ANALYSIS_CS,
    ENUM_ANALYSIS_RS
};
#endif

