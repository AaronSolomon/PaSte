#include <stdio.h>
#include "utils.h"

int inject(char* hiddenfile, char* keyfile, int protocal, int start, int size, char **data, int data_size){
}

int mySelect(char* infile, char* outfile, char* keyfile, int protocal, int start, int size, char *data, int data_size){
}
